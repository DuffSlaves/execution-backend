
if [ ! -f ".status/ghcjs" ]; then
	apt-get -qq -y install ghc cabal-install
	ghc --version
	# Install ghcjs
	cabal update
	PATH=$PATH:~/.cabal/bin
	cabal install base http://ghcjs.luite.com/master.tar.gz --reinstall
	ghcjs-boot
fi
