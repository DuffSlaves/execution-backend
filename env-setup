# Install git
apt-get install git

# Install java8
apt-add-repository -y ppa:webupd8team/java
apt-get update
apt-get install -y nodejs npm oracle-java8-set-default oracle-java8-installer

# Install GHC
apt-get install -y ghc ghc-prof ghc-doc

# Install google-closure-compiler
npm install --save google-closure-compiler

# Install ghcjs
cabal install happy alex
cabal install http://ghcjs.luite.com/master.tar.gz
ghcjs-boot

# Install PHP compilers
npm install phptoast phptojs phpruntime phpcommon

# Install emscripten
wget "https://s3.amazonaws.com/mozilla-games/emscripten/releases/emsdk-portable.tar.gz"
tar xzf emsdk-portable.tar.gz
rm emsdk-portable.tar.gz
cd emsdk_portable
./emsdk update
./emsdk install latest
./emsdk activate latest
source ./emsdk_env.sh
cd ../

